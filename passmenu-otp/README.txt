duct taped passmenu with `pass otp` integration:

1. makesure your otp scratch codes are imported with `pass otp` under
`.password-store/otp` path
2. replace passmenu with passmenu-otp

for arch users simply git clone and `makepkg -si` (this is too stupid to go to
    the AUR, lmao)
